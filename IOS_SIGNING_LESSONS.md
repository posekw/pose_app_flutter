# دليل توثيق أخطاء وحلول توقيع تطبيقات iOS (iOS Build Signing Lessons)

هذا الملف هو مرجع شامل لكل العقبات التي واجهناها أثناء إعداد البناء التلقائي (GitHub Actions) لتطبيق Flutter، وكيفية تجنبها مستقبلاً.

---

## 1. مشكلة الشهادات (Certificates)
*   **الخطأ:** انتهاء صلاحية الشهادة أو إلغاؤها (Revoked) من قِبل أبل.
*   **الدرس:** الشهادة من نوع **Apple Distribution** هي شهادة حساسة. عند إصدار شهادة جديدة، يجب حذف القديمة وتحديث ملف الـ `.p12` فوراً. 
*   **تنبيه:** لا تخلط بين شهادة التطوير (Development) وشهادة النشر (Distribution).

## 2. مشكلة الربط (Certificate-Profile Link)
*   **الخطأ:** البناء يفشل برسالة "Provisioning profile does not include signing certificate".
*   **السبب:** إنشاء شهادة جديدة دون تحديث "ملف التعريف" (Provisioning Profile) ليرتبط بها.
*   **الحل:** دائماً بعد إنشاء شهادة جديدة، ادخل على Profile -> Edit -> اختر الشهادة الجديدة -> Download.

## 3. مشكلة الأكواد الطويلة (Base64 Truncation)
*   **الخطأ:** فشل فك التشفير في GitHub برسالة "not enough data" أو "problem decoding".
*   **السبب:** كود الـ Base64 للشهادة والملف طويل جداً (أكثر من 8000 حرف). النسخ واللصق اليدوي قد يقص نهاية الكود.
*   **الحل:**
    1. استخدام سكربت بايثون لاستخراج الكود في ملف نصي مستقل (`.txt`).
    2. استخدام `Ctrl+A` لنسخ النص كاملاً.
    3. تحديث نظام GitHub Workflow ليستخدم **Python** لفك التشفير بدلاً من الأوامر العادية لأن بايثون أقدر على التعامل مع النصوص الضخمة.

## 4. مشكلة المعرف الفريد (UUID Mismatch)
*   **الخطأ:** Xcode يقول "No profile matching UUID found".
*   **السبب:** عند تحميل ملف تعريف جديد، يتغير رقم الـ **UUID** الخاص به. ملف إعدادات المشروع (`project.pbxproj`) يبحث عن الرقم القديم.
*   **الحل:** يجب استخراج الـ UUID من الملف الجديد وتحديثه في ملف `project.pbxproj` في الخانتين:
    *   `PROVISIONING_PROFILE`
    *   `PROVISIONING_PROFILE_SPECIFIER`

## 5. مشكلة إعدادات التصدير (ExportOptions.plist)
*   **الخطأ:** فشل مرحلة الـ Archiving.
*   **السبب:** عدم تطابق اسم الحزمة (Bundle ID) أو الفريق (Team ID) أو اسم ملف التعريف داخل ملف `ExportOptions.plist`.
*   **الحل:** تأكد من أن `provisioningProfiles` داخل الملف يحتوي على الـ Bundle ID الصحيح وقيمة الـ UUID الجديدة.

---

## ✅ دليل الخطوات السريعة لأي مشروع جديد:
1. أصدر شهادة **Distribution** وحولها لـ **.p12** بكلمة مرور `123456`.
2. أنشئ **Provisioning Profile** واربطه بالشهادة أعلاه حصراً.
3. استخدم سكربت استخراج الأكواد لضمان نظافة بيانات الـ **Base64**.
4. حدّث الـ **UUID** الجديد في ملف `project.pbxproj`.
5. ارفع الأكواد لـ **GitHub Secrets** مع حذف القديم أولاً.

---
*تم إعداد هذا التوثيق لضمان سلاسة العمل في المشاريع القادمة.* 🚀🏆
