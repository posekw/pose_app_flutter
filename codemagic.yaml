definitions:
  scripts:
    - &clean_and_setup
      name: Clean and Setup Environment
      script: |
        echo "=== STEP 1: Deep Clean - DerivedData & Caches ==="
        rm -rf ~/Library/Developer/Xcode/DerivedData
        rm -rf ~/Library/Caches/CocoaPods
        rm -rf ~/Library/Developer/Xcode/Archives
        
        echo "=== STEP 2: Installing Latest CocoaPods ==="
        sudo gem install cocoapods
        pod --version
        
        echo "=== STEP 3: Flutter Deep Clean ==="
        flutter clean
        rm -rf ios/build
        rm -rf ios/.symlinks
        rm -rf ios/Pods
        rm -rf ios/Podfile.lock
        rm -rf build
        
        echo "=== STEP 4: Get Packages ==="
        flutter pub get
        
        echo "=== STEP 5: Pod Install with Repo Update ==="
        cd ios
        pod repo update
        pod install --repo-update
        cd ..
        
        echo "=== Environment Ready! ==="

workflows:
<<<<<<< HEAD
  # Clean iOS build - removes problematic plugins before building
  ios-clean-build:
    name: iOS Clean Build
=======
  ios-build:
    name: iOS Build
>>>>>>> android
    max_build_duration: 60
    instance_type: mac_mini_m2
    integrations:
      app_store_connect: codemagic
    environment:
      ios_signing:
        distribution_type: app_store
        bundle_identifier: com.posekw.gallery
      flutter: stable
      xcode: latest
      cocoapods: 1.16.2
    triggering:
      events:
        - push
      branch_patterns:
<<<<<<< HEAD
        - pattern: main
=======
        - pattern: ios
>>>>>>> android
          include: true
    scripts:
<<<<<<< HEAD
      - name: Update CocoaPods
        script: |
          echo "Installing latest CocoaPods..."
          sudo gem install cocoapods
          pod --version
      - name: Deep Clean
        script: |
          echo "Cleaning Flutter project..."
          flutter clean
          rm -rf ios/Pods
          rm -rf ios/Podfile.lock
          rm -rf ios/.symlinks
          rm -rf ~/Library/Developer/Xcode/DerivedData
          echo "Clean complete!"
      - name: Remove Problematic Plugins
        script: |
          echo "Removing webview_flutter and url_launcher..."
          # Comment out problematic plugins in pubspec.yaml
          sed -i.bak 's/^  url_launcher:/#  url_launcher:/' pubspec.yaml
          sed -i.bak 's/^  webview_flutter:/#  webview_flutter:/' pubspec.yaml
          cat pubspec.yaml | grep -E "(url_launcher|webview_flutter)"
          echo "Plugins commented out!"
      - name: Get Flutter packages
        script: flutter pub get
      - name: Pod install
        script: |
          cd ios
          pod repo update
          pod install
          cd ..
=======
      - *clean_and_setup
>>>>>>> android
      - name: Set up code signing
        script: xcode-project use-profiles
      - name: Build iOS IPA
        script: |
          echo "=== Building IPA in RELEASE mode ==="
          flutter build ipa --release --export-options-plist=/Users/builder/export_options.plist
          echo "=== Build Complete! ==="
    artifacts:
      - build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log
    publishing:
      email:
        recipients:
          - c.ronaldo-mut@hotmail.com
        notify:
          success: true
          failure: true

<<<<<<< HEAD
  # Original workflow (for when privacy manifest issue is fixed)
  ios-release:
    name: iOS Full Build
    max_build_duration: 60
    instance_type: mac_mini_m2
=======
  android-build:
    name: Android Build
    max_build_duration: 60
    instance_type: mac_mini_m2
    environment:
      flutter: stable
      xcode: latest
>>>>>>> android
    triggering:
      events:
        - push
      branch_patterns:
<<<<<<< HEAD
        - pattern: ios-stable
          include: true
    environment:
      flutter: stable
      xcode: latest
      cocoapods: default
    scripts:
      - name: Update CocoaPods
        script: sudo gem install cocoapods
      - name: Deep Clean
        script: |
          flutter clean
          rm -rf ios/Pods
          rm -rf ios/Podfile.lock
          rm -rf ios/.symlinks
          rm -rf ~/Library/Developer/Xcode/DerivedData
      - name: Get Flutter packages
        script: flutter pub get
      - name: Pod install
        script: |
          cd ios
          pod repo update
          pod install
          cd ..
      - name: Set up code signing
        script: xcode-project use-profiles
      - name: Build IPA
        script: flutter build ipa --release --export-options-plist=/Users/builder/export_options.plist
    artifacts:
      - build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log
=======
        - pattern: android
          include: true
        - pattern: main
          include: true
    scripts:
      - name: Get Packages
        script: flutter pub get
      - name: Build Android APK
        script: |
          echo "=== Building APK in RELEASE mode ==="
          flutter build apk --release
          echo "=== Build Complete! ==="
    artifacts:
      - build/app/outputs/flutter-apk/*.apk
>>>>>>> android
    publishing:
      email:
        recipients:
          - c.ronaldo-mut@hotmail.com
        notify:
          success: true
          failure: true
